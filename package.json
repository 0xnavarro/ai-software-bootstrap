{
  "name": "ai-software-bootstrap",
  "version": "0.1.0",
  "private": true,
  "description": "Opinionated bootstrap for disciplined AI-assisted software engineering.",
  "type": "module",
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10 <11"
  },
  "scripts": {
    "check:package-manager": "node scripts/check-package-manager.mjs",
    "lint": "node scripts/check-js.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "test:architecture": "node scripts/check-architecture.mjs",
    "verify": "pnpm check:package-manager && pnpm lint && pnpm test && pnpm test:architecture"
  },
  "license": "Apache-2.0"
}
